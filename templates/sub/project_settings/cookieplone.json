{
  "schema": {
    "properties": {
      "domain": {
        "type": "string",
        "default": "{{ hostname }}",
        "format": "computed"
      },
      "image_prefix": {
        "type": "string",
        "default": "ghcr.io/codesyntax",
        "format": "computed"
      }
    }
  }
}