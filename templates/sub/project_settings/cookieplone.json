{
  "schema": {
    "properties": {
      "postgres": {
        "type": "boolean",
        "title": "Use Postgres?",
        "default": false,
        "format": "computed"
      },
      "thumbor": {
        "type": "boolean",
        "title": "Should we use Thumbor for image scale generation?",
        "description": "",
        "default": false,
        "validator": ""
      },
      "security_key": {
        "type": "string",
        "title": "Security Key",
        "default": "{{ random_ascii_string(32) }}",
        "format": "computed"
      }
    }
  }
}
